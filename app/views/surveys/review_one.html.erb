<% @reviews.each do |review| %>
<p><%= review.content  %>  <p>
	<% end %>

	<% if params[:team_id] %>
	<%= link_to "Back In Business", team_surveys_path(Team.find(params[:team_id]))%>
	<% elsif params[:survey_id] %>
	<%= link_to "Back In Business", survey_path(Survey.find(params[:survey_id]))%>
	<% else %>
	<%= link_to "Back In Business", root_path %>
	<% end %>